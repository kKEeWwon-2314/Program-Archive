interface firstInterface {

    // attributes initialized in an interface are always final
    int my_num = 5;

    int MOUSE_LEFT = 0;
    int MOUSE_RIGHT = 1;

    // avoid declaring attributes in an Interface 

    void printGreeting();
}